import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    btn: {
        backgroundColor: 'blue',
        margin: 10,
        padding: 10,
        borderRadius: 10,
    },
    btn_text: {
        color: 'white',
        textAlign: 'center',
    },
});